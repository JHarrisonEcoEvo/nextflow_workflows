machine:
    services:
        - docker

dependencies:
    override:

test:
    override:
        - docker pull nf_se_demux_to_bam_bai_denovo
        - curl -fsSL get.nextflow.io | bash
        - ./nextflow run nextflow run gbs_nextflow --in ./data/sample1.fastq --key data/samplekey.csv --path_out ./data/ --dbpath ./contaminants/ --mode denovo --threshold 4 
